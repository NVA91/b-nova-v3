all:
  hosts:
    pve01:
      ansible_host: 192.168.2.77
      ansible_user: admin
      ansible_ssh_private_key_file: ~/.ssh/id_ansible_service
      ansible_ssh_common_args: '-o IdentitiesOnly=yes -o StrictHostKeyChecking=no'

app_hosts:
  hosts:
    # Example app host (this should be the LXC/VM running Docker, not the Proxmox node)
    # nova-app:
    #   ansible_host: 192.168.2.100
    #   ansible_user: admin
    #   ansible_ssh_private_key_file: ~/.ssh/id_ansible_service
    #   ansible_ssh_common_args: '-o IdentitiesOnly=yes -o StrictHostKeyChecking=no'