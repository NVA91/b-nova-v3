all:
  children:
    proxmox_servers:
      hosts:
        pve01:
          ansible_host: 192.168.2.77
          ansible_user: admin
          ansible_ssh_private_key_file: ~/.ssh/id_ansible_service
          ansible_ssh_common_args: '-o IdentitiesOnly=yes -o StrictHostKeyChecking=no'

          # Single Source of Truth: load full hardware profile from versioned YAML
          hardware_profile: "{{ lookup('file', playbook_dir + '/inventory/hardware/pve01.yml') | from_yaml }}"

    app_hosts:
      hosts: {}
