---
# Ansible Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  # AWX/Ansible Tower Collection (required for controller.yml playbook)
  # Installed from GitHub due to Galaxy resolution issues
  # Alternative: Install from Galaxy when available: ansible.awx:>=21.0.0
  - name: https://github.com/ansible/awx.git#/awx_collection/
    type: git
    version: devel
